import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Prof en Poche',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
